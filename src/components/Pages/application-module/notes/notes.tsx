"use client";

import ActivityWorkspace from "@/components/Pages/kalao/ActivityWorkspace";

const NotesComponent = () => (
  <ActivityWorkspace type="note" title="Notes" addLabel="Nouvelle note" />
);

export default NotesComponent;
